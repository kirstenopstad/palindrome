namespace Palindrome
{
  public class Checker
  {
    // Get user input (string)
    // Store string as array

    // Iterate through array 
      // make all lowercase 
      // create a reverse copy array

    // Iterate through length and check reverse against original array
      // If all are ==, return true
      // Else return false 

    // NOTES
    // "Kirsten" false || "Brian" false || "Abba" true
    // "123" false || "456" false || "323" true
    // "0abba0" true
    // properties, constructors, methods, etc. go here
  }
}
